# Google API key
GOOGLE_API_KEY=<YOUR_GOOGLE_API_KEY>

# Local prompt file path inside the analyzer container (relative or absolute)
PROMPT_FILE=prompts/agent_prompt.md

# Path to the directory with .sol files to analyze (mounted into /data)
TARGET_PATH=test/THORChain20210723/source-code

# Output file for analysis results inside container
OUTPUT_FILE=output/results.json

# Optional: verbosity / debug
LOG_LEVEL=INFO
